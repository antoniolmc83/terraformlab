node {

  stage('Scm') {
    checkout scm
  }
 
  dir('aws-demo') {

 
    stage('Init') {
      echo 'Terraform init'
      sh 'terraform init'
    }

    stage('Plan') {
      echo 'Terraform plan'
      sh 'terraform plan'
    }
  }
}

